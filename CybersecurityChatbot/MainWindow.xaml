<Window x:Class="CybersecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CybersecurityChatbot"
        Title="CipherGuard Lupus" Height="840" Width="1280" Background="#070B16">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#0F172A" BorderBrush="#1E293B" BorderThickness="0,0,1,0">
            <DockPanel Margin="14">
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                    <Image Height="92" Source="Images/lupuschatlogo.jpg" Stretch="Uniform"/>
                    <TextBlock Text="CipherGuard Lupus" Foreground="#E2E8F0" FontWeight="Bold" FontSize="20" Margin="0,12,0,0"/>
                    <TextBlock Text="AI-style Cybersecurity Assistant" Foreground="#94A3B8" FontSize="12"/>
                </StackPanel>

                <StackPanel>
                    <Button Style="{StaticResource NavButton}" Click="HomeNav_Click" Content="Home"/>
                    <Button Style="{StaticResource NavButton}" Click="ChatNav_Click" Content="Chatbot"/>
                    <Button Style="{StaticResource NavButton}" Click="QuizNav_Click" Content="Quiz"/>
                    <Button Style="{StaticResource NavButton}" Click="GameNav_Click" Content="Memory Game"/>
                    <Button Style="{StaticResource NavButton}" Click="SchedulerNav_Click" Content="Task Scheduler"/>
                    <Button Style="{StaticResource NavButton}" Click="HistoryNav_Click" Content="Progress / History"/>
                    <Button Style="{StaticResource NavButton}" Click="SettingsNav_Click" Content="Settings"/>
                </StackPanel>

                <TextBox DockPanel.Dock="Bottom" Text="{Binding AsciiArt}" FontFamily="Consolas" Foreground="#60A5FA" Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="250"/>
            </DockPanel>
        </Border>

        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>

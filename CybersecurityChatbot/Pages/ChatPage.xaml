<Page x:Class="CybersecurityChatbot.Pages.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="#0B1220">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <ListBox x:Name="Messages" Background="#0F172A" Foreground="#E2E8F0" BorderThickness="0"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox x:Name="Input" Width="620" Height="36" KeyDown="Input_KeyDown"/>
            <Button Content="Send" Margin="10,0,0,0" Width="100" Click="Send_Click"/>
        </StackPanel>
    </Grid>
</Page>

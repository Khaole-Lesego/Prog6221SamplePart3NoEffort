<Page x:Class="CybersecurityChatbot.Pages.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:CybersecurityChatbot"
      Background="#0B1220">
    <Page.Resources>
        <DataTemplate x:Key="UserMessageTemplate">
            <Border Background="#4F46E5" CornerRadius="14" Padding="12" Margin="70,4,0,4" HorizontalAlignment="Right">
                <StackPanel>
                    <TextBlock Text="You" Foreground="#C7D2FE" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Text}" Foreground="White" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Time}" Foreground="#C7D2FE" FontSize="10" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="BotMessageTemplate">
            <Border Background="#1E3A5F" CornerRadius="14" Padding="12" Margin="0,4,70,4" HorizontalAlignment="Left">
                <StackPanel>
                    <TextBlock Text="🐺 Lupus" Foreground="#93C5FD" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Text}" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Time}" Foreground="#93C5FD" FontSize="10" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </DataTemplate>
        <local:MessageTemplateSelector x:Key="MsgTemplateSelector"
            UserTemplate="{StaticResource UserMessageTemplate}"
            BotTemplate="{StaticResource BotMessageTemplate}"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="12" Padding="12">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="Messages" ItemTemplateSelector="{StaticResource MsgTemplateSelector}"/>
                </ScrollViewer>
                <TextBlock x:Name="TypingText" Grid.Row="1" Text="🐺 Lupus is thinking..." Foreground="#60A5FA" Visibility="Collapsed" Margin="4,8,0,0"/>
            </Grid>
        </Border>

        <DockPanel Grid.Row="1" Margin="0,10,0,0">
            <TextBox x:Name="Input" Height="40" FontSize="14" VerticalContentAlignment="Center" KeyDown="Input_KeyDown"/>
            <Button Content="Send" Width="110" Margin="10,0,0,0" Click="Send_Click"/>
        </DockPanel>
    </Grid>
</Page>
